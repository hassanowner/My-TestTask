![banner-review](main-banner.png)

# JavaScript Course – Comprehensive Educational Review

## Overview :
This repository contains a **comprehensive collection of JavaScript test tasks** designed for learning, revising, and practicing core JavaScript concepts. Each topic has dedicated exercises and challenges to help reinforce understanding.  

The purpose of this repository is purely **educational**:  
- To serve as a **revision tool**.  
- To act as a **quick reference guide** for JavaScript fundamentals.  
- To provide **hands-on practice** with real examples.  

> !! This is not a production project. It's intended as a **learning and practice resource**.!!

---

## Structure & Topics 🤖

### 1. Data Types & Variables ( Part 1 & Part 2 )
- **Data Types and `typeof` Operator** – Identifying different data types in JavaScript.  
- **Variables Introduction** – Declaring and initializing variables.  
- **Identifiers: Name Convention and Rules** – Guidelines for naming variables and functions.  
- **Var, Let, Const and Comparison** – Differences in variable declarations and behavior.  
- **String Syntax and Character Escape Sequences** – Handling strings and special characters.  
- **Concatenation** – Joining strings using `+`.  
- **Template Literals (Template Strings)** – Modern string interpolation using backticks.  
- **Variables & Concatenation Challenges** – Exercises to practice combining variables and strings.  
- **Arithmetic Operators** – `+`, `-`, `*`, `/`, `%`, etc.  
- **Unary Plus and Negation Operators** – Converting and negating numbers.  
- **Type Coercion** – Automatic type conversion.  
- **Assignment Operators** – Shorthand operators like `+=`, `-=`, `*=`, etc.  

---

### 2. Numbers & Strings ( P1 & P2 )
- **Number Basics** – Working with number types.  
- **Number Methods** – Useful methods like `toFixed`, `parseInt`, `parseFloat`.  
- **Math Object** – Built-in math functions and constants.  
- **Number Challenge** – Practical exercises for numbers.  
- **String Methods Part 1, 2, 3** – Methods to manipulate and analyze strings.

---

### 3. Conditionals & Logic ( P1 & P2 )
- **Comparison Operators** – `==`, `===`, `>`, `<`, `>=`, `<=`, etc.  
- **Logical Operators** – `&&`, `||`, `!`.  
- **If Condition** – Basic conditional execution.  
- **Nested If Condition** – Conditions inside conditions.  
- **Conditional Ternary Operator** – Short-hand for `if-else`.  
- **Nullish Coalescing Operator (`??`)** – Handling `null` or `undefined`.  
- **If Condition Challenge** – Practice exercises.  
- **Switch Statement** – Multi-branch selection structure.

---

### 4. Arrays ( P1 & P2 )
- **Array Big Introduction** – Basics and usage of arrays.  
- **Using Length With Array** – Checking array size.  
- **Add and Remove From Array** – `push`, `pop`, `shift`, `unshift`.  
- **Searching Array** – `indexOf`, `includes`, `find`.  
- **Sorting Array** – Sorting elements alphabetically or numerically.  
- **Slicing Array** – Extracting subarrays using `slice`.  
- **Joining Arrays** – Combining arrays with `concat` or `join`.  

---

### 5. Loops ( P1 & P2 )
- **Loop For and the Concept of Loop** – Basic for-loop.  
- **Looping on Sequences** – Iterating over arrays and ranges.  
- **Nested Loops and Trainings** – Loops inside loops for complex tasks.  
- **Loop Control: Break, Continue, Label** – Controlling loop flow.  
- **Loop For Advanced Example** – Advanced exercises.  
- **Practice – Add Products to Page** – Practical DOM loop exercises.  
- **Loop While** – While-loop practice.  
- **Loop Do, While** – Do-while loop practice.  

---

### 6-7. Function Basic 
- **Function Intro and Basic Usage** – Declaring and calling functions.  
- **Function Advanced Examples** – More complex examples and practice.  
- **Function Return and Use Cases** – Returning values from functions.  
- **Function Default Parameters** – Using default values.  
- **Function Rest Parameters** – Handling variable number of arguments.  
- **Function Ultimate Practice** – Consolidated function exercises.  
- **Random Arguments Function Challenge** – Practice with variable arguments.  
- **Anonymous Function and Use Cases** – Functions without names.  
- **Return Nested Function** – Functions returning other functions.  
- **Arrow Function Syntax** – Modern ES6 function shorthand.  
- **Scope: Global and Local** – Understanding variable scope.  
- **Scope: Block** – Block-level scope with `let` and `const`.  
- **Scope: Lexical** – Lexical scope and closures.

---

### 8–13. Functions Tasking
- Normalize Names
- Clean User Data
- Classify Element
- Random Argument Handler
- Analyze Array Data

---
### 14-18. Functional Methods Mix
- **Map Exercises** – This file contains practical tasks focused on the **`map`** method, transforming arrays and creating new arrays based on existing data.  
- **Filter Exercises** – Here, learners practice using **`filter`** to select specific elements from arrays according to conditions.  
- **Reduce Exercises** – This file introduces **`reduce`** for aggregating values, such as sums, products, or combined results.  
- **ForEach Exercises** – Tasks in this file focus on **`forEach`** to iterate over arrays and perform side-effects or accumulate results manually.  
- **Combined Mix** – The final file combines **`map`**, **`filter`**, **`reduce`**, and **`forEach`** in realistic scenarios, showing how these functional methods can be used together for complex data processing.

----
### 19–21. Object Concepts Overview
- Object Basics: Creating objects, accessing properties, dynamic keys.
- Nested Objects & Methods: Deep structures, methods, and `this`.
- Object Creation Patterns: `new Object()`, `Object.create()`, `Object.assign()`.
- Mini Final Task: Inheritance, merging objects, dynamic properties, custom methods.
- Objects + Functions Mix: Using functions to process and update object data.

---
### 22. DOM – Document Object Model ( P1 & P2 & P3 & P4 )
- **DOM Selectors**  
  Selecting elements using IDs, classes, tags, and query selectors.

- **Get / Set Content & Attributes**  
  Reading and modifying element content and attributes.

- **Check Attributes**  
  Checking, updating, and removing element attributes.

- **Create Elements (Basic)**  
  Creating new elements and injecting them into the DOM.

- **Create Product (Heading + Paragraph)**  
  Dynamically creating a structured container with a title and description.

- **Deal With Children**  
  Accessing children, nodes, and first/last element relationships.

- **DOM Events**  
  Handling user, window, and input events via JS or HTML.

- **Form Validation & Prevent Default**  
  Validating inputs and preventing default form or link actions.

- **Event Simulation**  
  Triggering events programmatically like click, focus, blur.

- **DOM ClassList**  
  Managing CSS classes using add, remove, toggle, and other methods.

- **DOM CSS Manipulation**  
  Changing inline styles and CSS properties with JavaScript.

- **Deal With Elements**  
  Inserting, removing, and positioning elements in the DOM.

- **DOM Traversing**  
  Navigating parents, siblings, and child elements.

- **DOM Cloning**  
  Cloning elements with or without child nodes.

- **Add Event Listener + Cloned Element Handling**  
  Adding event listeners and handling events on original and cloned nodes.

---
### 23. BOM – Browser Object Model (P1, P2, P3, P4)

- **Window Object + Dialogs + Timers**  
  Window Object (global features, size, URL, properties)  
  alert / confirm / prompt (user messages & inputs)  
  setTimeout + clearTimeout (delayed actions)  
  setInterval + clearInterval (repeated actions)

- **Location, Window Control, Popups, History**  
  Location Object (read URL, change URL, reload)  
  Window Open / Close (popups, custom size)  
  History API (back, forward navigation)  
  Window Controls (stop, print, focus, scrollTo, scrollBy)  
  Scroll API + Scroll-To-Top Button (smooth scrolling, UI tools)

- **Local Storage + Practice**  
  Local Storage Basics (setItem, getItem, removeItem, clear, key)  
  Local Storage Practice (theme save, persistent UI)

- **Session Storage (Per Tab Storage)**  
  Session Storage (temporary tab data, auto-restore, auto-clear)
---
## How to Use 🚧
1. Open each JavaScript file in your preferred editor.  
2. Read the comments and instructions for each task.  
3. Run the code in **browser console** or **Node.js** to see outputs.  
4. Modify and experiment with the code to understand the concepts better.
5. You can access and review the organized list of all previously covered topics through the link provided here.
6. \-\-\> [about.md file](about.md)

## Purpose 📚
This repository is a **learning and revision toolkit**. It is designed for:  
- Quick **revision** of JavaScript concepts.  
- Practicing **coding exercises**.  
- Serving as a **reference for variables, loops, arrays, strings, and functions**.  

> 📌 Consider this repository as a **personal study reference** rather than a production-ready project.

---

**😄 Happy Learning and Practicing JavaScript!**
